#!/usr/bin/env ruby

require 'telegram/bot'

require './lib/message_responder'
require './lib/app_configurator'
require './lib/yaml_parser'
require './lib/request'

config = AppConfigurator.new

token = config.get_token
logger = config.get_logger

logger.debug 'Starting telegram bot'

Telegram::Bot::Client.run(token) do |bot|
	unless request.between?(940, 963)
		bot.api.send_message(chat_id: 105879286, text: "Быстро перейдите на #{Configurations::URL}, регистрация скорее всего началась!!!")
	end

	# 105879286
	# 184164761

  bot.listen do |message|
		puts message.chat.id

    logger.debug "@#{message.from.username}: #{message.text}"
    MessageResponder.new({bot: bot, message: message}).respond
  end
end
